package com.example.demo.domain.enums;

/** Loại thẻ học. Dùng chung cho DeckItems và UserCardStates. */
public enum StudyItemType {
    VOCAB,
    KANJI,
    /** Thẻ người học tự soạn (bảng CustomCards). */
    CUSTOM
}
