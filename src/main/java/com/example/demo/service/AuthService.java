package com.example.demo.service;

// TODO: Implement authentication logic
// - authenticateUser(LoginRequest) → validate credentials, generate JWT
// - registerUser(RegisterRequest)  → save new User, hash password (BCrypt)
// - refreshToken(String)           → validate refresh token, issue new access token
// - handleOAuth2Login(OAuth2User)  → find-or-create user from Google profile
public interface AuthService {

}
